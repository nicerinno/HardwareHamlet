<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hardwarehamlet.hardwarehamlet">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".builds.BuildsDetails"></activity>
        <activity android:name=".builds.BuildsList" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".components.ComponentsList" />
        <activity
            android:name=".NavActivity"
            android:label="@string/title_activity_nav" />
        <activity android:name=".Home" />
        <activity android:name=".Rankings" />
        <activity android:name=".builds.Build" />
        <activity android:name=".components.ComponentsDetails" />
        <activity android:name=".builds.AddBuild" />
        <activity android:name=".builds.PickComponents" />
        <activity android:name=".builds.BuildMakerFragment" />
        <activity android:name=".builds.ComponentListToPick" />
    </application>

</manifest>